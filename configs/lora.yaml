# @package lora
# Fields other than `enabled` are forwarded to peft.LoraConfig.
enabled: false

r: 8
lora_alpha: 16
lora_dropout: 0.05
bias: "none"
task_type: CAUSAL_LM
target_modules: ".*language_model\\.layers\\..*\\.(q_proj|k_proj|v_proj|o_proj|gate_proj|up_proj|down_proj)"
